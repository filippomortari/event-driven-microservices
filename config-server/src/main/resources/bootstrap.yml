spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/workspace/event-driven-microservices/config-server-repository
          clone-on-start: true
    fail-fast: true